Checkpoints will be released soon. 
In the meantime, you can use the scripts in `pretrain_models` to train your own models.
Pre-training is used to train a model for the task it will perform after deployment, for example cardiomegaly classification.